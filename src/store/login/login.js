import Vue from 'vue'

Vue.use(Vuex)

const  state = {
  }
const getters = {
  }
const mutations = {
  }
const actions = {
  }
const modules = {
  }


export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations,
}